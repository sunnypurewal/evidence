name: PR Build

on:
  pull_request:
    branches: [main]
  merge_group:

permissions:
  contents: read

jobs:
  pr-build:
    uses: RiddimSoftware/riddim-release/.github/workflows/swift-package-build.yml@main
