# evidence

Repeatable proof that app flows work, captured from real iOS app runs instead of manual replay.

`evidence` is planned as a Swift package and companion CLI for generating screenshots, App Store assets, preview video sources, and PR build-evidence artifacts from declarative app evidence plans.

The initial backlog lives in Jira project RIDDIM under the `init-evidence-open-source` initiative label.
